@model KonigLabs.Models.BlogMeta

<div class="col-md-3 col-md-offset-1">
    <aside>
        <div class="input-group">
            <form action="@Url.Action(MVC.Home.Blog())">
                <input class="form-control" placeholder="Поиск по блогу" name="search" id="s" type="text" value="@Model.SearchValue">
                <button style="margin-top:5px;">Искать</button>
            </form>
        </div>

        <div class="pad30"></div>
        <h6>О чем этот блог</h6>
        <p>
            Реализация намеченных плановых заданий влечет за собой процесс внедрения и модернизации позиций, занимаемых участниками в отношении поставленных задач.
        </p>

        <div class="pad10"></div>

        <h6>Категории</h6>

        <ul class="fa-ul categories">
            @foreach (var cat in Model.Categories)
            {
                <li><i class="fa-li fa fa-angle-right"></i><a href="@Url.Action(MVC.Home.Blog(string.Empty, null, null, cat.Id, string.Empty))">@cat.Name</a></li>
            }
        </ul>

        <div class="pad25"></div>

        <h6 class="pad25">Тэги / Метки / Зарубки</h6>
        @foreach (var tag in Model.Tags)
        {
            <span class="badge"><a href="@Url.Action(MVC.Home.Blog(string.Empty, null, tag.Id, null,string.Empty))">@tag.Name</a> @tag.Count</span>
        }
    </aside>
</div>